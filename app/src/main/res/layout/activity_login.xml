<?xml version="1.0" encoding="utf-8"?>
<!-- RelativeLayout allows for element positioning with
respect to other elements (parent or sibling) -->
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".LoginActivity">

    <!--can center vertically within parent with relativeayout-->
    <TextView
        android:id="@+id/helloWorld"
        android:text="GradeCalculator"
        android:textSize="30dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"/>

    <!--relativelayout, can lay out in respect to element by id-->
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/helloWorld"
        android:layout_centerHorizontal="true"
        android:text="Log in"/>

</RelativeLayout>
